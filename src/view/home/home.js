import { View, Text } from 'react-native';

const Home = ({ navigation }) => {
    return(
        <View>
            <Text>
                SUA PLAYLIST AQUI
            </Text>
        </View>
    );
}

export default Home;